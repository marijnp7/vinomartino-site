server {
    listen 80;
    server_name vinomartino.travel preview.vinomartino.com;

    root /usr/share/nginx/html;
    index index.html;

    add_header X-Content-Type-Options nosniff;
    add_header X-Frame-Options SAMEORIGIN;
    add_header Referrer-Policy strict-origin-when-cross-origin;

    # LAT-879: container listens on :80 but is exposed on :8090 (vinomartino.travel:8090).
    # Without this, `return 301 /...` emits an absolute Location with the internal :80 port
    # stripped → dev redirects land on prod (vinomartino.travel:80).
    absolute_redirect off;

    error_page 404 /404.html;

    location ~* /_astro/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # LAT-799 legacy multi-brand redirects: bookmarks/index entries → /
    location ~ ^/vinomartino(/.*)?$ {
        return 301 /$is_args$args;
    }
    location ~ ^/nl(/.*)?$ {
        return 301 /$is_args$args;
    }
    location ~ ^/bestemmingen(/.*)?$ {
        return 301 /$is_args$args;
    }

    location / {
        try_files $uri $uri/index.html $uri.html =404;
    }
}
