diff --git a/.github/workflows/deploy-cos.yml b/.github/workflows/deploy-cos.yml
index b1467cc..7d872a5 100644
--- a/.github/workflows/deploy-cos.yml
+++ b/.github/workflows/deploy-cos.yml
@@ -47,6 +47,11 @@ jobs:
           Host $VPS_HOST
             StrictHostKeyChecking accept-new
             UserKnownHostsFile ~/.ssh/known_hosts
+            ServerAliveInterval 30
+            ServerAliveCountMax 6
+            TCPKeepAlive yes
+            ConnectTimeout 20
+            ConnectionAttempts 5
           EOF
           ssh-keyscan "$VPS_HOST" >> ~/.ssh/known_hosts 2>/dev/null || true
 
diff --git a/.github/workflows/deploy.yml b/.github/workflows/deploy.yml
index 6277151..1f2e430 100644
--- a/.github/workflows/deploy.yml
+++ b/.github/workflows/deploy.yml
@@ -30,6 +30,13 @@
 
 name: Deploy VinoMartino
 
+# LAT-3298: dispatch-inputs zijn achteraf niet via de API opvraagbaar. Zonder
+# dit kun je aan de runlijst niet zien of een run prod raakte.
+run-name: >-
+  Deploy → ${{ (github.event_name == 'workflow_dispatch' && inputs.target != 'auto' && inputs.target)
+  || (github.ref == 'refs/heads/main' && 'production' || 'preview') }}
+  (${{ github.ref_name }}, ${{ github.event_name }})
+
 on:
   push:
     branches: [preview, main]
@@ -72,12 +79,22 @@ on:
       - ".dockerignore"
   workflow_dispatch:
     inputs:
+      # LAT-3298/LAT-3294: NIET meer `default: preview`. Die default maakte de
+      # `elif`-tak hieronder onbereikbaar, waardoor `gh workflow run --ref main`
+      # zonder expliciete `-f target` naar PREVIEW deployde en tóch groen
+      # afliep (run 30700769553). `auto` spiegelt nu het push-gedrag: de branch
+      # bepaalt het doel, precies zoals bij een push naar main.
       target:
-        description: "Doel (preview of production)"
-        required: true
-        default: preview
+        description: "Doel — 'auto' volgt de branch (main → production)"
+        required: false
+        default: auto
         type: choice
-        options: [preview, production]
+        options: [auto, preview, production]
+      force_preview_from_main:
+        description: "Sta preview-deploy vanaf main toe (normaal geweigerd)"
+        required: false
+        default: false
+        type: boolean
 
 concurrency:
   group: vinomartino-write
@@ -96,12 +113,25 @@ jobs:
       - name: Determine target environment
         id: env
         run: |
-          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
-            target="${{ github.event.inputs.target }}"
-          elif [ "${{ github.ref }}" = "refs/heads/main" ]; then
-            target="production"
-          else
-            target="preview"
+          # LAT-3298: branch-default. 'auto' (of geen input) volgt de ref,
+          # zodat dispatch- en push-gedrag identiek zijn.
+          target="${{ github.event.inputs.target }}"
+          if [ "${{ github.event_name }}" != "workflow_dispatch" ] \
+             || [ -z "$target" ] || [ "$target" = "auto" ]; then
+            if [ "${{ github.ref }}" = "refs/heads/main" ]; then
+              target="production"
+            else
+              target="preview"
+            fi
+          fi
+
+          # LAT-3294: main+preview mag niet meer STIL groen aflopen. Wie het
+          # echt wil, zet force_preview_from_main=true en ziet dat terug in
+          # de run-name.
+          if [ "${{ github.ref }}" = "refs/heads/main" ] && [ "$target" = "preview" ] \
+             && [ "${{ github.event.inputs.force_preview_from_main }}" != "true" ]; then
+            echo "::error::Dispatch op main met target=preview raakt productie NIET en zou groen aflopen (LAT-3294). Gebruik target=auto/production, of zet force_preview_from_main=true."
+            exit 1
           fi
 
           if [ "$target" = "production" ]; then
@@ -141,6 +171,11 @@ jobs:
           Host $VPS_HOST
             StrictHostKeyChecking accept-new
             UserKnownHostsFile ~/.ssh/known_hosts
+            ServerAliveInterval 30
+            ServerAliveCountMax 6
+            TCPKeepAlive yes
+            ConnectTimeout 20
+            ConnectionAttempts 5
           EOF
           # Best-effort host-key prefetch; non-fataal als netwerk flakey is
           ssh-keyscan "$VPS_HOST" >> ~/.ssh/known_hosts 2>/dev/null || true
diff --git a/.github/workflows/promote.yml b/.github/workflows/promote.yml
index 73c05f1..f4f6f0a 100644
--- a/.github/workflows/promote.yml
+++ b/.github/workflows/promote.yml
@@ -40,6 +40,11 @@ jobs:
           Host $VPS_HOST
             StrictHostKeyChecking accept-new
             UserKnownHostsFile ~/.ssh/known_hosts
+            ServerAliveInterval 30
+            ServerAliveCountMax 6
+            TCPKeepAlive yes
+            ConnectTimeout 20
+            ConnectionAttempts 5
           EOF
           ssh-keyscan "$VPS_HOST" >> ~/.ssh/known_hosts 2>/dev/null || true
 
diff --git a/.github/workflows/publish.yml b/.github/workflows/publish.yml
index 1ea2dd3..bdb38fb 100644
--- a/.github/workflows/publish.yml
+++ b/.github/workflows/publish.yml
@@ -58,6 +58,11 @@ jobs:
           Host $VPS_HOST
             StrictHostKeyChecking accept-new
             UserKnownHostsFile ~/.ssh/known_hosts
+            ServerAliveInterval 30
+            ServerAliveCountMax 6
+            TCPKeepAlive yes
+            ConnectTimeout 20
+            ConnectionAttempts 5
           EOF
           ssh-keyscan "$VPS_HOST" >> ~/.ssh/known_hosts 2>/dev/null || true
 
@@ -202,6 +207,11 @@ jobs:
           Host $VPS_HOST
             StrictHostKeyChecking accept-new
             UserKnownHostsFile ~/.ssh/known_hosts
+            ServerAliveInterval 30
+            ServerAliveCountMax 6
+            TCPKeepAlive yes
+            ConnectTimeout 20
+            ConnectionAttempts 5
           EOF
           ssh-keyscan "$VPS_HOST" >> ~/.ssh/known_hosts 2>/dev/null || true
 
@@ -273,6 +283,11 @@ jobs:
             DECISION="pending"
             for i in $(seq 1 120); do
               sleep 5
+              # LAT-3769: houd het SSH-kanaal hoorbaar. Een volledig stille
+              # sessie werd na ~4,5 min ge-evict (run 30980347827).
+              if [ $((i % 12)) -eq 0 ]; then
+                echo "… nog steeds pending ($((i * 5)) s van max 600 s)"
+              fi
               STATUS=$(curl -sS "$COS_URL/approval/$APPROVAL_ID" || echo '{}')
               DECISION=$(echo "$STATUS" | python3 -c "
             import sys,json
