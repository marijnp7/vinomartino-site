---
// LAT-859 + LAT-989: Redirect /routes/ to /wijnroutes/ (canonical URL structure)
export async function getStaticPaths() {
  return [{ params: {} }];
}

return Astro.redirect('/wijnroutes/', 301);
---
