---
// LAT-2693 — NL streken-overzicht (prefixloos, DEFAULT_LOCALE). Markup + logica
// zitten in het gedeelde StrekenIndex-component; de /en/-tegenhanger
// (src/pages/en/streken/index.astro) rendert hetzelfde component met locale="en".
// De listing-index bestaat altijd in beide talen, dus hreflang wijst wederzijds.
import StrekenIndex from '../../components/StrekenIndex.astro';
---

<StrekenIndex alternates={{ nl: '/streken/', en: '/en/streken/' }} />
