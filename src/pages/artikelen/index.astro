---
// LAT-2693 — NL artikelenoverzicht (prefixloos). Markup, facet-filter en logica
// leven in het gedeelde ArtikelenIndex-component; de /en/-tegenhanger rendert
// hetzelfde met locale="en".
import ArtikelenIndex from '../../components/ArtikelenIndex.astro';
---

<ArtikelenIndex alternates={{ nl: '/artikelen/', en: '/en/artikelen/' }} />
