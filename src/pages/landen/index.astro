---
// LAT-2709 — NL landen-overzicht (prefixloos, DEFAULT_LOCALE). Markup + logica
// zitten in het gedeelde LandenIndex-component; de /en/-tegenhanger
// (src/pages/en/landen/index.astro) rendert hetzelfde component met locale="en".
// De listing-index bestaat altijd in beide talen, dus hreflang wijst wederzijds.
import LandenIndex from '../../components/LandenIndex.astro';
---

<LandenIndex alternates={{ nl: '/landen/', en: '/en/landen/' }} />
